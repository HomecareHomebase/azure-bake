name: e2e-package
shortName: e2e
version: 1.0.0
resourceGroup: false
parallelRegions: false
ingredients:
  - "@azbake/fixture-script@1.0.0"
  - "@azbake/ingredient-null@1.0.0"
recipe:
  run-script:
    properties:
      type: "@azbake/fixture-script"
      source: ./
      parameters:
        outputPath: ./e2e.out
        message: hello
  noop:
    properties:
      type: "@azbake/ingredient-null"
      source: ./
      parameters:
        ping: "[ctx.Environment.environmentCode]"