name: test-package
shortName: tstpkg
version: 1.0.0
ingredients:
  - "@azbake/ingredient-null@~0"
variables:
  foo: config
  bar: config
recipe:
  alpha:
    properties:
      type: "@azbake/ingredient-null"
      source: ./src
      parameters:
        foo: bar