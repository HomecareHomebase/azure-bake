name: test-package
shortName: tstpkg
version: 1.0.0
ingredients:
  - "@azbake/fixture-ingredient@1.2.3"
variables:
  foo: config
  bar: config
recipe:
  alpha:
    properties:
      type: fixture
      source: ./src
      parameters:
        foo: bar