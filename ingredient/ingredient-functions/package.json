{
  "name": "@azbake/ingredient-functions",
  "description": "Ingredient for deploying a function app from a docker image.",
  "version": "0.1.176",
  "main": "dist/index.js",
  "typings": "src/index.ts",
  "author": "HCHB",
  "license": "MIT",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "upload": "tsc -p . && npm --no-git-tag-version version patch && npm publish --access public",
    "compile": "tsc -p .",
    "watch": "tsc -w -p .",
        "test": "mocha --timeout 30000 -r ts-node/register/transpile-only ../../tests/ingredient-contracts.test.ts test/**/*.test.ts"
  },
  "directories": {
    "lib": "dist"
  },
  "peerDependencies": {
    "@azbake/core": "*"
  },
  "devDependencies": {
    "@azbake/core": "^0.1.108",
    "@azbake/ingredient-utils": "^0.1.113",
    "@types/node": "^22.13.0"
  },
  "dependencies": {
    "@azbake/arm-helper": "^0.1.112",
    "azure-bake": "^0.1.95"
  },
  "publishConfig": {
    "access": "public"
  }
}
