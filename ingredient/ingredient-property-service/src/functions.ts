import { BaseUtility, IngredientManager } from '@azbake/core'

export class PropertyServiceClientUtils extends BaseUtility {

    public async get_property(name: string): Promise<string> {
        return "JOE";
    }
}

