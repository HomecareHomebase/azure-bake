name: bus-test
shortName: bustest
version: 1.0.0
ingredients:
  - "@azbake/ingredient-azure-vm"
resourceGroup: true
rgOverride: test
parallelRegions: false
variables:
  vmName: testvm
  adminUsername: testuser
recipe:
  namespace:
    properties:
      type: "@azbake/ingredient-service-bus-namespace"
      parameters:
        vmName: "[coreutils.variable('name')]"
        adminUsername: "[coreutils.variable('adminUser')]"