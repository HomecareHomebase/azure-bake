{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@types/node": "^10.12.18",
    "lerna": "^3.10.1",
    "typescript": "^3.2.2"
  },
  "scripts": {
    "bootstrap": "lerna clean --yes && lerna bootstrap --hoist && lerna run compile"
  }
}